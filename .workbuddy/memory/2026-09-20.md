# 2026-09-20 工作日志

## 宁德时代(300750)方案核查与 ROIC/WACC 口径修复（第 7 轮）

### 方案四条指控判定

| # | 指控 | 判定 |
|---|---|---|
| 1 | ROIC 91.74% 是计算错误 | **真缺陷成立**，但根因猜错（不是 ROA 错写） |
| 2 | 缺「竞争格局」维度 | 拒绝：市占率/专利数/单Wh利润/产能利用率全库无取数链路（同银行 NPL 案例）；方案数字均为手填常数；industry 模块已有同业相对位（宁德 100A，peer 107） |
| 3 | WACC≈12% 偏高 | **数字属实但来自另一条 bug**：DCF 路径用含应付 DR 63.65%→12%，盈利模块用有息 12.79%→7%，两处口径分叉 |
| 4 | 应收/存货/OCF 风险提示不足 | 大部分已覆盖或数字有误：官方应收天数 53.57（方案称 95，错）；OCF/净利 1.39 健康本就无需告警；存货/营收 47% 是 H1 流量口径错配（年化 23.6%）；产能利用率无管道 |

### 根因 A：现金奶牛分母坍缩 → ROIC 爆炸

`_invested_capital_series`：invested = 权益 2899 + 有息 1169 − **货币资金全额 3335** = 732 亿
（占总资产 7.5%）→ ROIC 91.74%，2024 曾 112.97%。现金越充裕分母越小、数值越爆炸。

**外部锚定**：中国证券报披露宁德 2026H1 投入资本回报率 8.37%（年化≈16.7%）；按全资本口径
复算 2025 年报 = 16.5%，完全吻合 → 91.74% 确属口径错误。

### 根因 B：WACC 两处独立计算口径分叉

- `_estimate_wacc_pct`（盈利模块）：有息负债率优先（docstring 明写「与引擎 DCF 对齐」）
- `engine._dynamic_wacc`（DCF）：只收含应付 `debt_ratio` → 宁德 63.65% → 12%
- DCF 在 `composite_valuation_score`（engine.py:1053）之后才构建（:1060），**不影响综合分**，仅展示参考值

### 改动

| 文件 | 改动 |
|---|---|
| `modules/profitability.py` | `_invested_capital_series` 超额现金抵扣上限 = 有息资本 50%；ROIC 块新增全资本口径 `roic_gross_capital_pct` 进 metadata |
| `engine.py` | `_dynamic_wacc` 改为委托 `_estimate_wacc_pct`（单一来源），入参补 `interest_bearing_ratio`/`business_model`；DCF 调用点传入两者 |

### 实测（修前 → 修后）

| 标的 | ROIC | 全资本口径 | 综合 | profit 分 |
|---|---|---|---|---|
| 300750 宁德时代 | 91.74 → **33.02** | 16.51 | 84.2（不变，ROIC 分带饱和） | 82.6（不变） |
| 600519 贵州茅台 | 42.75（不变，现金大头在拆出资金未映射） | 34.02 | 84.4 | 90.0 |
| 000651 格力电器 | 24.4 → 22.22（现金占资本 54.5% 恰过线） | 11.43 | 70.7（不变） | 86.8 |
| 000062 深圳华强 | 4.33（不变，分销基准仍有效） | 3.50 | 61.3（不变） | 65.0 |
| 600036 招商银行 | 10.66（银行无货币资金映射，不变） | 10.66 | 79.9（不变） | 85.0 |
| 300408 三环集团 | 12.36（不变） | 10.59 | 81.7（不变） | 86.8 |
| 300975 商络电子 | 6.36（不变） | 5.24 | 62.9（不变） | 76.1 |

宁德 DCF：WACC 12% → 7.0%，内在价值 877 元/股 vs 现价触发 **3 倍护栏 `is_reliable=False`**（系统自带保护生效）。

### 测试

新增 3 项：`test_invested_capital_caps_excess_cash_deduction` /
`test_roic_metadata_for_cash_rich_company_has_gross_capital_reference` /
`test_dcf_wacc_single_source_with_profitability`（测试文件头部补 `import pytest`）。
全量：**299 passed, 5 failed**（5 项仍是既有拼音搜索/板块扫描环境问题，与本轮无关）。**尚未部署。**

### 保留脚本

`backend/_diag_catl.py <symbol>`（dump ROIC 分子分母/投入资本/回退序列，通用）。

### 方案外发现（未动，供参考）

- 界面新闻：宁德 OCF +2.61% vs 净利 +42%（剪刀差扩大）、应付票据及应付账款半年 +918.8 亿、
  供应商融资余额 577.6 亿(+55%)（反向保理 133→373.7 亿）、合同负债同比持平 vs 营收 +54.8% ——
  系统的成长/现金流模块均未捕捉这些「链条占款」信号。
- 存货 1308 亿（+38.4%，库存商品 +111%）媒体提示减值风险，risk.py 无存货逻辑；
  未加新阈值（无同业锚点，拍脑袋违反铁律 11）。

## 第 8 轮方案核查（engine.py 四项指控）

### 判定

| # | 指控 | 判定 |
|---|---|---|
| 1 | V 型/边际改善信号在 growth→valuation 传递中丢失（`_gm` 笔误） | **不采纳：已实现**。engine.py:267-271 三个 key 全部写回 meta，`_gm` 前后一致无笔误；估值模块 ：717-718 消费。方案引用的「修改前代码」是旧版本幻觉 |
| 2 | 周期股溢价因子与 dividend_valuation_factors 冗余 | **采纳（真缺陷）**，但理由修正：「红利定价锚」对利差≥2.8% 再 +2，而利差本身已是 40% 权重因子（≥2.5% 得 88）= 同一事实重复计分；「资源壁垒溢价」与 PE/PB 分位重复计入「便宜」维度，且关键词（矿/磷/农化）对煤炭等行业名不生效——实测神华/陕煤/兖矿/中煤/云天化/淮北矿业六只无一触发，近乎死代码；breakdown 展示 88/85 分与实际贡献（+4/+2 不进加权）口径不符（铁律 7）。周期股分红承诺已有独立加分（dvf 内 +5），无需外部溢价 |
| 3 | `_base_fcf` 非高成长负 FCF 用 OCF×0.3 应改 0.2 | **不采纳**：0.2 与 0.3 同为经验系数无外部校准；DCF 仅在无相对估值信号回退路径影响综合分；负 FCF 已在 cashflow 模块真实惩罚。换常数不构成修复 |
| 4 | ctx 漏传 symbol/industry/name | **不采纳：已实现**。engine.py:228-230 三个字段都在，且注释里就写着漏传 symbol 的历史事故（铁律 8） |

### 改动

- `engine.py`：移除 `_run_valuation` 红利分支内的周期股溢价块（原 ：863-882），留注释说明三条理由。
- `tests/test_analysis_engine.py`：新增 `test_cycle_dividend_premium_block_removed`（磷化工+股息率 7% 高利差场景，断言 breakdown 无两溢价项且红利四因子框架正常）。
- 全量：**300 passed, 5 failed**（既有环境问题）。六只周期股修后重跑，溢价项均未触发，分数差异为行情/PE 数据漂移（首轮云天化 eastmoney 估值接口还失败过一次）。**尚未部署。**

### 新增诊断脚本

`backend/_diag_cycle.py`：批量跑周期/红利股，dump 溢价因子命中与 is_div 状态。

## 部署（2026-09-20 09:25，第 7+8 轮一起上线）

- push.ps1 退出码 1（已知 Tee-Object 坑），服务器 `~/candle-flow-deploy.tgz` 已清理 = 脚本完整跑完。
- 进程启动 09:25:40 > engine.py mtime 09:11:21，新代码已加载；`/api/v1/health` 正常。
- 服务器端抽样与本地一致：神华 83.7 A- / 宁德 84.2 A-（全资本 ROIC 16.51，第 7 轮修复生效）/ 华强 61.3 C / 招行 79.9 B+。
- 公网 candle-flow.online 健康检查与分析接口均正常。

## 第 9 轮方案核查（海螺水泥：价值陷阱 vs 红利龙头矛盾）

### 判定

- 方案称的「红利豁免缺失」「WACC 未按红利口径调整」**两条建议均已实现**：
  豁免在 engine.py `_apply_value_trap_veto`（is_dividend_asset 时剔除「ROIC低于WACC」理由）；
  红利 WACC=4.5%（`DIVIDEND_ASSET_WACC_PCT`，比方案建议的 5% 更低）。
- **但方案描述的矛盾在线上真实存在，是真缺陷**——且与方案猜的根因不同：
  同一代码本地 73.4 B / 线上 60.7 C。根因是铁律 9 同股两判：
  股息率回退（东财估值接口失败时用 D0/股价）只写在 `_run_valuation` 内部，
  而盈利/风险/现金流模块经 ctx 拿到的 dividend_yield 仍是 None →
  盈利模块判「非红利」（WACC 7%、ROIC 4.6%<WACC → veto 锁 28），
  估值模块自己回退算出 5.0% 判「红利」+ 长线信号「红利龙头底仓」。
  数据可用性决定分类结果 = 数据管道缺陷，不是阈值问题。

### 改动

| 文件 | 改动 |
|---|---|
| `engine.py` | 股息率回退上移到 `run_full_analysis`（ctx 构建前，:199 后），`_run_valuation` 内原回退删除留注释 |
| `tests/test_analysis_engine.py` | 新增 `test_dividend_yield_fallback_shared_across_modules`：mock 东财失败，断言盈利/估值同判红利 + wacc 4.5 + 无 veto |

### 实测

- 全量 301 passed（5 既有环境失败）。
- 本地复跑海螺：恰逢东财失败走回退 5.0% → 全模块同判红利、veto=None、估值 82.3、综合 73.4 B。
- 修后预期线上：海螺 60.7 C → ~73 B（veto 解除）。**尚未部署。**

## 部署（2026-09-20 09:59，第 9 轮红利分类同源修复上线）

- push.ps1 干净通过（`SETUP_OK` / 退出码 0）；TGZ 已清理；进程启动 09:59:55 > engine.py mtime 09:39:00。
- **海螺水泥 600585：60.7 C → 73.4 B**（val 82.3、veto 解除、prof_div=val_div=True、wacc 4.5），本地/服务器/公网三方一致。
- 对照未波及：神华 83.7 A-（不变）、深圳华强 61.3 C（不变，div 双 False、wacc 10.0 分销口径）；招行 80.8（+0.9，行情漂移）。
- 验证要点：**同代码本地值从此可作为线上预期**——此前本地 73.4 / 线上 60.7 的分叉已消除。

## 第 10 轮：全市场扫描方案核查（市场扫描功能优化）

### 方案四条主张的判定

| 主张 | 判定 |
|---|---|
| 全市场股票列表 / 批量并行拉取 / 基础过滤 / 五维因子 / 定时调度 | **已实现**：`stock_universe`（5783）、`factor_db.build_all`（8 线程 + 20s/票超时 + 批预算）、`_to_symbol` 剔 ST/BJ/ETF、`run_full_analysis` 五维、`_scheduled_sync` 工作日 16:35 |
| 五维权重 25/20/20/15/20 | **拒绝**：系统权威口径是 .20/.16/.16/.32/.16，另叠风险乘数与事件扣分；照搬会造成同股两分 |
| 百分位标准化 / 行业中性化 / Top50 排序输出 | **真缺口**（原 `factor_snapshots` 只写不读）→ 已补读层 |
| 审计意见 / 日均成交额过滤、DuckDB、Tier1+Tier2 | **拒绝**：前两者无取数链路（`audit_opinion` 只是公告标题 hint，默认「标准无保留」→ 假过滤）；后两者分别无收益/已有 Layer1-4 |

### 真实瓶颈（方案完全未提）

- 线上 `factor_snapshots` 仅 **1112 / 5382 = 20.7%**，日志 `factor build BATCH DEADLINE 1800s reached, 4274 remaining`（2026-09-18）。
- 单只 ≈13s（1112 只 × 8 并发 / 1800s）× 全市场 5382 只 ≈ **2.4h**，30 分钟预算必然截断 → 方案「几分钟完成 4000+ 只」的假设不成立。
- 非披露期只补缺失条目 → 可多日渐进；补齐入口本轮已参数化。

### 改动

| 文件 | 改动 |
|---|---|
| `services/market_scan.py`（新建） | 读层：`load_covered`（SQLite `json_extract` 取轻量字段，失败回退 Python 解析）、`percentile_rank`（平均秩，缺失不赋 50）、`scan_market`（过滤 + 排序 + 分位展示 + 口径声明）、`market_coverage` |
| `api/v1/fundamentals.py` | 新增 `GET /fundamentals/market-scan`、`GET /fundamentals/market-coverage`；`rebuild_factors` 支持 `budget_sec` / `max_symbols` |
| `services/factor_db.py` | `build_all` 支持 `budget_sec`/`max_symbols`，回传 `coverage`；预算改由 env `FACTOR_BUILD_DEADLINE_SEC` 可调（默认仍 1800s） |
| `tests/test_market_scan.py`（新建，5 项） | 分数透传 + 覆盖率自证 + 分位基准；行业/市值/综合分过滤；维度排序为展示且缺失不赋中性值；JSON1 回退；build_all 批上限与覆盖率 |

### 实测

- 全量 **306 passed, 5 failed**（与上轮同样的 5 个历史遗留：4 拼音 + `test_bull_tactics.py::test_scan_market_filters_main_board`，后者名字含 scan_market 但与新模块无关）。
- 用线上真实 1112 条快照在本地跑读层（`_diag_scan.py`）：**综合分与 payload 逐位一致**（未重算）；Top = 东方铁塔 86.8 / 芭田股份 86.6 / 亚钾国际 86.3 / 茅台 86.0 / 神华 85.6；`exclude_st=True` 匹配 1058（剔除 54 只 ST/退）。
- 快照滞后 2.02 天 → 榜单继承因子库新鲜度，已在 `coverage.stale_days` 自证。
- **尚未部署。**

## 前端榜单页（第 10 轮后续，用户：加一个扫描榜单页）

- 新增 `frontend/src/views/MarketScanView.vue` + 路由 `/market-scan` + Header 导航「基本面榜单」；`api/index.ts` 增加 `MarketScanItem/Coverage/Data` 类型与 `fetchMarketScan/fetchMarketCoverage`（字段与 `scan_market` 返回体逐一对齐）。
- 页面结构：覆盖率自证横幅（不完整/滞后时显式警告 + 补齐命令提示）→ 筛选器（排序维度/行业/综合分/市值/条数/剔除ST）→ 榜单表（排名/代码名称/行业/综合分 pills/评级/五维列/分位条+行业内分位/PE/PB/市值/股息率/详情→`/chart/:symbol`）→ 口径 notes。
- 设计取舍：排序只用 `composite_score` 与五维（不新增第二套权重）；分位仅展示；详情链到 ChartView（内嵌 FundamentalPanel）；**没做页面内「补齐缺口」按钮**——rebuild 是长阻塞请求（预算可达小时级），过隧道必超时，页面只给命令提示。
- 验证：`vite build` 通过（MarketScanView 9.1KB chunk）；`vue-tsc` 15 个错误全部为既有（DashboardView 10 / FundamentalPanel 5），新文件零错误；本地 uvicorn(8000)+vite(5173) + agent-browser 实测——从线上取 Top200 真实快照注入本地库渲染，默认榜 / 按估值排序+行业筛选（高亮列生效）/ 暗色主题三种状态截图确认；验完已还原 factor_snapshots=0 并删除 _seed200.json/_scan_filtered.png/_scan_dark.png，保留 `frontend/_scan_page.png` 供预览。
- 工具坑：agent-browser daemon 不跨工具调用保留，交互链必须同一 bash 调用内 `&&` 串联；截图参数 `--full`；select 用 value 不用 label；可执行文件在 `C:\Users\14149\.npm-global\agent-browser.cmd`。
- **尚未部署**（dist 已构建好，push.ps1 会带上 `frontend/dist`）。


## 部署上线（第 10 轮：市场扫描读层 + 前端榜单页）

- `push.ps1` 干净通过（SETUP_OK / 退出码 0，34s）；服务进程 11:02:31 晚于源文件 10:25:26。
- 验证：`market_scan.py` 已上线；dist/index.html 10:33:54 为本轮构建产物；`MarketScanView-eg5kbVF3.js` 公网 200（9931B）。
- `GET /fundamentals/market-scan?top=5` 线上实测：matched 1058 / base 1112，Top5 东方铁塔 86.8 A、芭田 86.6、亚钾 86.3、茅台 86.0、神华 85.6，评级/股息率/PE/分位字段全部透出。
- 公网 `https://candle-flow.online/market-scan` 浏览器实测：标题「基本面榜单」、覆盖率横幅（20.7% 警告 + 补齐命令提示）、榜单与 notes 渲染完整，无 console 错误。
- 验证坑复认：① ssh 内联 python 引号嵌套会炸 → 必须 heredoc 写 /tmp 再传；② Windows Git Bash `curl -o /dev/null` 的 size_download 恒为 0 → 落临时文件再 stat。
- 线上因子库覆盖率仍 20.7%（4270 缺口，快照滞后 2.05 天），榜单页已如实展示；补齐命令 `POST /fundamentals/factors/rebuild?budget_sec=10800`（约 2h），未代跑。

## 因子库补齐启动（用户确认代跑）

- 根因答复：批预算 1800s vs 全市场 2.4h（单只 ≈13s）→ 首轮只建了 1112；「补缺优先」增量逻辑让存量缺口 4270 一直没人补。
- 11:07 服务器端 `nohup curl POST /fundamentals/factors/rebuild?budget_sec=10800`（pid 135334）。实测 8 并发 ≈2.5s/只（快照 1112→1144 / ~80s），预计 3h 内补满 5382。
- 已建一次性自动化（14:25）检查结果：remaining>0 自动续跑第二轮；remaining=0 验证榜单+公网并汇报。

## 第 11 轮：三层漏斗叠加到基本面榜单（技术共振层）

- 方案判定：三层漏斗内核**全部已存在**——`evaluate_confluence`（趋势/动量/波动/量价/结构正交共振 + 周线趋势多周期确认）、`PatternEngine`（Nison 蜡烛+西方指标）、`_detect_buy_signal`（MA20/60+量比+PEG 买点）。方案给的 AlphaFeed 库不存在也不需要（K线走本地库）；共振权重公式（趋势×1.5…）拒绝——沿用 `_combined_score = 形态分+有效共振数×6` 单一口径。行业共振以板块统计展示（≥2 只才列）。
- 真缺口：`market_confluence_service.scan_market` 只跑基本面分层，`_scan_job` 成死代码；榜单页无技术面。落地 `market_scan.technical_overlay()`：复用 `_scan_job`（口径逐位一致）+ `_detect_buy_signal`，8 并发，40 只 1.8s，10min 缓存；API `GET /fundamentals/market-scan/technical-overlay?top≤120`。
- **连带真缺陷修复**：engine `run_full_analysis` 返回 dict 漏 `profit_yoy`（只在内部 ctx）→ 快照无此字段 → confluence 服务 PEG 恒 None → **强买入信号此前永不可达**。已补返回键；旧快照缺失时 PEG 降级保守（overlay stats 自证 peg_available）。
- 数据验证：线上 K线库 3077 只/78 万根（最新 9-18），榜单 Top60 中 53 只有 ≥40 根（88%）。
- 前端：MarketScanView 加「叠加技术面」开关 + 买点信号列 + 形态共振列 + 信号计数/板块效应条 + 「仅看有信号」筛选；api 加 `MarketScanOverlayFields/Data` + `fetchMarketScanOverlay`。agent-browser 实测：40 只 1.8s，9 只命中形态共振，渲染完整。
- 测试：+3（overlay 聚合/缓存、无K线 insufficient、强买入缺 PEG 不可达）→ 全量 309 passed / 5 failed（既有）。
- **尚未部署**。注意：部署重启会中断进行中的因子补齐（已建快照保留）；14:25 的自动化检查会发现 remaining>0 并自动续跑（续跑即用新代码，新快照自带 profit_yoy）。
- 工具坑：agent-browser `click "button.btn-secondary"` 会点到第一个同 class 按钮（刷新），改 `find text "叠加技术面" click`。

## 部署上线（第 11 轮：技术共振叠加层）

- **部署前置状态（15:2x 核查）**：11:07 触发的因子补齐已在 **14:16 自然结束**（`factor build BATCH DEADLINE 10800s reached, 23 remaining`），无需再等；服务曾于 15:18:49 自行重启过一次（应与补齐内存压力有关），当前 active/running。
- 前端 `vite build` 重新构建通过（MarketScanView-Cs17cQCg.js 14310B）。**注意**：`npx --no-install vite build` 会误落到 WSL shim（报「适用于 Linux 的 Windows 子系统没有已安装的分发」）→ 必须用 `node node_modules/vite/bin/vite.js build`。
- `push.ps1` 干净通过（`SETUP_OK` / TGZ 已清理）；服务进程 15:24:03 晚于源文件（market_scan.py 11:20 / engine.py 11:18）。
- 变更确认：`engine.py:229 profit_yoy` 已在返回体；`market_scan.py` OVERLAY_WORKERS=8 / `technical_overlay()` 在 :458。
- **覆盖率 20.7% → 96.8%**（covered 5211 / universe 5382 / remaining 171 / stale_days 0.01）；榜单 Top：吉比特 89.8 A、睿创微纳 89.4 A、三美股份 88.7 A。
- 公网 `candle-flow.online` 三方一致：`market-coverage` 与 `technical-overlay?top=3` 均 200，榜单页 root=200。

### ⚠️ 遗留真缺口：profit_yoy 存量快照永不回填

- `build_all(force=False)` 的跳过条件是「symbol 已有快照」→ **不看 payload 是否含新字段**。11:07~14:16 那轮用的是旧 engine（未带 profit_yoy），因此 **5211 条存量快照全部缺该键**；`main_board_kline_sync` 每日 16:35 调的是 `build_all()`（force=False），只会补那 171 只缺失的 → 其余永远拿不到 profit_yoy。
- 后果：overlay 的 PEG 恒 None → **「强买入」档位对存量股票不可达**（实测吉比特 kline_bars=90 但 peg=null）；形态共振/买点/中性档不受影响。`_profit_yoy_map` 已按铁律 16 保守降级为 None（不虚构增速），overlay `stats` 自证。
- 两条修复路径（成本相同 ≈3~3.5h 外部 API）：① `POST /fundamentals/factors/rebuild?force=true&budget_sec=10800` 一次性全量重建；② 先把 `build_all` 非 force 分支的跳过条件改成「快照存在 **且** payload 含必需字段」（schema 守卫，未来加字段可自愈），再跑一次。
- 今天是周日（非工作日）→ 16:35 定时构建不会触发，不影响重建；重建期间不要部署重启。

### 环境坑复认（本轮新增）

- **沙箱会拦网络**：sandboxed PowerShell 下 `ssh` 报 `Connection timed out during banner exchange`（TCP 层 Test-NetConnection 反而 True）、公网 https 返回 530 —— 全部是沙箱假象，不是服务器故障。部署/ssh/公网验证一律 `dangerouslyDisableSandbox=true`。
- **PowerShell 内联 ssh 命令的引号会炸**（嵌套 `"` 与 `()`）→ 改为：本地写 `.sh` → `Get-Content x.sh -Raw | ssh host "bash -s"`（可正常回显，注意脚本行尾 CRLF 会报 `$'\r'` 但无碍）。
- 本地 git 仓库根在 `D:\candle-flow(2)\candle-flow`（外层），`candle-flow/candle-flow` 内的 .git 不是生效仓库；`git` 在 `D:\Program Files\Git\cmd\git.exe`。

## 部署上线（第 11 轮补丁：因子库 schema 守卫 + profit_yoy 回填）

- 用户选定方案：**加 schema 守卫 + 重建**（而非只跑 force=true）。
- 实现（`services/factor_db.py`）：
  - 新增 `_REQUIRED_SNAPSHOT_KEYS = ("profit_yoy",)` + `_outdated_snapshot_symbols()`：用 `json_type(payload, '$.k') IS NULL` 判定**键不存在**（不是值为 null——合法空值不该反复重建）；SQLite 无 JSON1 时回退 Python 解析 payload。
  - `_get_stale_symbols(existing, all_symbols, outdated)` 新增第三参：非披露期返回「缺失 ∪ 缺键」；顺手把返回顺序从 set 乱序改成 `sorted()`。
  - `build_all` 非 force 分支调用守卫并打日志 `N snapshots outdated by schema guard`。
- 新增 2 个测试：`test_build_all_schema_guard_rebuilds_snapshots_missing_new_keys`（缺键重建 / 值为 null 不重建 / skipped=1）、`test_outdated_snapshot_symbols_tolerates_broken_payload`（伪造 SQL 失败验证回退解析）。**全量 311 passed / 5 failed**（既有）。
- 部署（15:29:59 重启，`SETUP_OK`），守卫已在服务器确认（factor_db.py:173/176/189/265）。
- 15:30 触发 `POST /rebuild?budget_sec=10800`（**非 force**，故意用它验证守卫在生产真的选出 5382）：实测回填速率 **≈0.43 只/秒**（`with_profit_yoy` 8→72 / 150s），全量预计 **~3.5h**（15:30 → ~19:00）。
- 已建一次性自动化（19:45）验证回填结果：查 with_profit_yoy/total、coverage complete、overlay 的 peg_available 与 buy_signal，并汇σ报；未跑完则顺延 40 分钟再查（不重复触发）。
- 回填基线快照：total 5211 / with_profit_yoy **0**（确证存量全缺该键）。

## K 线覆盖诊断（用户：为啥 K 线这么少）→ 维持现状

- 现象：overlay 横幅「K线可用 31/50」。
- 实测定论：`kline_data` 3078 只 / 78.2 万根 / 最新 9-18（数据新鲜完整），但**只收沪深主板非 ST**——`_main_board_symbols` 用战法扫描的 `is_main_board`（前缀 600/601/603/605 + 000/001/002/003）+ `is_st_name` 过滤。
- 因子快照 5211 只中 **2148 只无 K 线**：创业板 300/301/302 共 1392、科创板 688/689 共 611、ST/退 197、B股等 ~48。Top50 里缺的都是创业板/科创板/ST（如 688002 睿创微纳、301717 超纯应材 kline_bars=0）。
- 「最近形态」页同库 → 创业板/科创板/ST 在形态页也永不出现。**属设计口径，非缺陷。**
- 用户选择**维持现状**（不改 `_main_board_symbols`），页面继续如实标注 N/50。若日后要扩：改 `_main_board_symbols` 支持可配置板块即可，战法侧自带过滤不受影响。

## 榜单增强：名称搜索 + 默认剔除创业板/科创板（第 12 轮，已部署 15:58:52）

- 用户需求：榜单加名称搜索；默认不扫创业板/科创板，可选纳入。
- 后端 `market_scan.py`：`scan_market`/`technical_overlay` 新增 `keyword`（名称或代码子串，大小写不敏感）与 `include_gem`（默认 False=剔除 300/301/302/688/689，`GEM_STAR_PREFIXES` + `_is_gem_star`）；两键进 `filters` 自证，notes 增加口径说明。
- API 两端点各加 `keyword`/`include_gem` Query 参数；前端 api/index.ts 类型与参数透传。
- 前端 `MarketScanView.vue`：form 加 `keyword`/`include_gem`（复选框「含创业板/科创板」默认不勾，title 提示两板不在 K 线同步范围）；结果副标题显示「名称/代码含 X · 仅沪深主板」；overlay 缓存复用判断同步比较 keyword/include_gem。
- 测试：新增 `test_scan_keyword_and_gem_default_excluded`（默认剔除/勾选纳入/名称命中/代码命中/无命中自证）→ 全量 **312 passed / 5 failed**（既有）。
- 部署 15:58:52 重启（deploy_20260920_f.log，health ok）。**重启杀掉了 15:30 的回填线程**（进度 897/5211），16:00 已重触发非 force rebuild（守卫自动选剩余缺键），19:45 自动化继续兜底验证。
- 线上验证：keyword=茅台 命中 600519；默认榜 gem/star 泄漏 0；include_gem=true 命中含创业板/科创板；榜单页 200。

## 榜单增强：分页 + 股价 + 技术分析覆盖全页 + WAL 并发读修复（第 13 轮，已部署 16:18:38 / 16:28:11）

- 用户三个需求：榜单加分页；显示股价；技术共振要覆盖榜单**所有**股票（原来只覆盖前 120 只）。
- 分页：`scan_market` 新增 `offset`，返回 `offset/matched/has_more`；`top` 上限仍是 `MAX_TOP=500`。前端 `MarketScanView.vue` 加分页条（上一页/下一页/页码），翻页时若已开叠加自动为新页重算（单票缓存兜底）。
- 股价：快照 payload 的 `market.price` 进 SELECT 与 item（服务器覆盖 5204/5212）；榜单新增「股价」列，实测吉比特 347.71。
- 全量覆盖：`technical_overlay` 重写——**不再截断到 120**（`OVERLAY_PAGE_LIMIT = MAX_TOP`），本页每只都做技术分析；新增**单票级缓存** `_overlay_item_cache`（TTL 10min）+ 页面级缓存；`stats` 增加 `analyzed/computed/retried/failed` 自证。
- 实测成本：50 只 15.2s、120 只 40.3s（≈0.3s/只，且当时回填在抢 CPU，属悲观值）。
- **并发写竞争真缺陷**：旧库 `journal_mode=delete` + `busy_timeout=5s`，回填持写锁时读超时 → 科技叠加出现 **13/40「分析失败」**，且失败结果被写进 10 分钟缓存（固化错误）。
  - 修 1：`app/database.py` 对 sqlite 设 `PRAGMA journal_mode=WAL` + `busy_timeout=15000`（WAL 读写不互斥）。
  - 修 2：`_overlay_one` 失败标记 `failed=True`；失败结果**不进缓存**，并做一次**串行重试**；`stats.failed/retried` 自证。
  - 验证（回填进行中）：**analyzed 40 / computed 40 / failed 0 / retried 0**，`strong_buy` 出现 1 只、`peg_available` 18/40 —— 修复生效，且 PEG 回填已让强买入档可达。
- 单票复现脚本确认失败是锁而非代码：茅台单独跑 90 根 K 线、中性，正常。
- 全量测试 **314 passed / 5 failed**（既有拼音+板块扫描环境问题）。
- 遗留观察（未修）：写竞争下 `scan_market` 基础列表读取达 46s（json_extract 全表 5212 行 vs 写事务），缓存命中后仍需先付这次读取成本。回填结束后应回落；若成为常态再考虑把 keyword/过滤下推到 SQL。

## 方案诊断：基本面/技术面动态权重 + 三层漏斗（第 14 轮，未改代码）

- 用户提交一份外部方案（按投资周期加权 70/30、50/50、20-30/70-80；五维 25/20/20/15/20；技术面 趋势30/动量25/量价20/形态15/大盘10；`综合=基本面50%+技术面30%+资金面20%`；风格调整表）。
- **判定「已实现」**：三层漏斗的①②③与方案B共振过滤**早已落地**——第一层=榜单 `min_composite` + rating 表；第二层=共振组合分阈值 `CANDIDATE_COMBINED=80`（`market_confluence_service.py:40`）+ `_detect_buy_signal`；第三层=`technical_overlay` 的 `buy_signal` 档位（strong_buy/watch/short_term）。技术面维度系统已含 trend/momentum/volatility/volume/structure（`core/confluence.py` 维度映射），仅缺「大盘环境」。
- **判定「拒绝」**：
  1. 五维权重表 25/20/20/15/20 —— **第二套权重**（铁律 24）。系统权威是 `MODULE_WEIGHTS = 盈利.20/成长.16/偿债.16/现金流.32/估值.16`（`analysis/config/__init__.py:5`），现金流 2 倍权重是为短板否决刻意设计。可复算反例：其余四维 70 分、现金流 40 分（E 档×0.5=20）时，系统口径 54.0（D），方案权重 60.0（C）→ **自动升一档 = 放松短板惩罚**。方案还完全没提 `E_GRADE_PENALTY=0.5` / `CASHFLOW_VETO_THRESHOLD=40` 这两层。
  2. `综合=基本面50%+技术面30%+资金面20%` —— 再造第三个总分，会与个股分析的 `composite_score` 形成**同股两分**；且三维异构尺度直接加权无依据。
  3. **资金面不可落地**：全库无个股级主力净流入/北向持仓/机构席位取数（已 grep 确认无 `stock_individual_fund_flow`/`northbound`/龙虎榜）。只有 `api/v1/flow.py` 的**宽基**分时与 `fundamental_screen._fetch_sector_fund_flow()` 的**板块**资金流排名（akshare `stock_sector_fund_flow_rank`），口径不同且是盘后数据，塞进榜单等于新增外部依赖 + 破坏「盘中零 API 调用」。
  4. 阈值 ≥70/≥60/≥85 不需要改代码：系统已有 `min_composite` 过滤 + A(85)/A-(80)/B+(74)/B(70)/B-(65)/C(55)/D(40)/E 评级表。
- 提出**唯一诚实的落地方式**：榜单页加「策略风格」预设（长线/中线/短线），只打包筛选参数（min_composite、默认是否叠加技术面、板块范围、排序键），**不改任何分数权重**——满足用户"按周期调整"的意图而不产生第二套口径。已询问用户是否实施。

## 16:50 诊断：Dashboard 市场扫描慢的原因
- prescreen 层无逐只进度（只发 0/N 两个回调），前端长时间停在「已启动 0%」
- 因子库 profit_yoy 回填（budget_sec=10800，16:28 重启后 curl 重触发）与扫描 8 并发抢 2 核 CPU/网络
- 实测 job a63a5c33b65e：fundamentals 647/726 (89%)，~6min；因子库 miss 的票走 analyze_symbol_full 实时分析（13s/只）
- 结论：主要是资源竞争 + prescreen 无进度观感问题；回填结束后扫描会显著变快

## 17:35 共振视图上线（i 版）
- 基本面榜单改造为「共振扫描」：全局档位排序（核心持仓→买入候选→观察→淘汰），不按综合分排序
- 后端：market_scan.py 加 _base_items/_item_passes/_compute_tech_map 抽取 + resonance_index_build（后台 job，fund≥60 全量算技术面）+ resonance_view（读层过滤/判档/排序/分页）；fundamentals.py 加 3 个路由（GET resonance / POST build / GET progress）
- 前端：MarketScanView 默认 sort_by=resonance，档位 chip 服务端过滤，索引自触发构建+进度条；api/index.ts 加共振类型与 3 个函数
- 实测：索引 2049 只 53.8s 构建，0 失败；分布 core 5 / candidate 114 / watch 1137 / eliminated 1799
- 覆盖率失败为瞬时资源竞争（QueuePool/db locked），非 bug；profit_yoy 回填因部署重启已重触发
