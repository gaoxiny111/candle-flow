# 2026-09-22

## 服务器夜间重启恢复 + .4 重建续跑

- 全量测试最终结果：**389 passed / 5 failed / 1 skipped**（5 失败均为既有：拼音搜索 4 + 主板过滤 1），之前「499 canceled」是干扰，无新增回归。
- 服务器 47.100.175.214 于 09-22 08:33 重启（夜间自动重启）。症状：SSH banner 交换超时 + HTTPS 530（origin unreachable），ping 通。systemd 自动拉起 candle-flow 与 cloudflared。
- 误操作提醒：用 Python `sqlite3.connect()` 探询 `/opt/candle-flow/backend/data/factors.db` 会**凭空创建 0 字节空库**（真正快照表在 `candle_flow.db` 的 `factor_snapshots` 表）。已删除。
- 快照版本分布（重启后）：None 3640 / .1 508 / .3 275 / .4 831（昨晚 19:18 停）。已 POST `/api/v1/fundamentals/factors/rebuild?budget_sec=10800` 续跑（nohup curl 挂服务器后台，端点同步返回、结束才回统计）。
- **重启后线上验证左侧拦截依然生效**：002371 北方华创 → tech=None / 观察 / 左侧超跌观察，blocker 含决策日收盘 676.61 < MA60 724.61、量比 0.87<1.2。002409 雅克科技、603628 恒为科技不在 500 强榜单（更强淘汰，符合预期）。
- 健康检查精确路径：`/api/v1/health`（返回 {code,message,data}，含 db/akshare 状态）；覆盖率 `/api/v1/fundamentals/market-coverage`（重启后 97.5%，covered 5254/5388）。
- 已创建定时任务：每 3 小时检查版本分布并续跑 rebuild（validUntil 09-23），剩余约 4300 只按 5-8 只/分钟需 9-14h。

## 第七轮：金牛化工批判的核实与两处真修复（scoring_version .4 → .5）

外部批判（两轮）主张加「一票否决」熔断。**核实后方向不成立，但另有两处真缺口**。

### 核实结论：病根上轮已修，熔断规则会误杀半个市场

- 金牛化工病根 = `tier=turnaround` → 拿到「PE 极高不算贵」豁免 → `relative.PE_TTM.signal`
  被 `engine.py` 从「高估」强改为「合理」。**`.3/.4` 的 `CYCLE_TROUGH_PB_MAX=5.0`
  已掐断**（金牛 PB 8.49 被排除，实测 `is_growth_stock=False`；万华 PB 2.01 保留）。
  线上仍见洗白 = 该票是 09-20 旧快照，重建未覆盖。
- 外部三条熔断规则全库实测（5254 只）：周转率<0.5 命中 **55.8%**、ROE<5% 命中 **54.2%**、
  PE分位>85% 命中 **18.7%**；三条同时 6.7%（含大秦铁路/山西焦煤/钢研高纳）。
- 「PE分位≥90 且 PB分位≥90」判泡沫拦掉中国神华(87.2/A)、生益科技、深南电路、三环集团。
  **分位高 ≠ 泡沫**（优质公司盈利增长抬高估值中枢）。决定性反证：PE分位 95.6% 拦中国神华，
  却放走 PE分位 89.8% 的金牛化工 —— **单指标绝对阈值对本问题完全无效**。
- 有效判据 = **指标间的矛盾组合**，不是单指标绝对值。

### 修复 A：现金流微利稀释封顶（modules/cashflow.py）

`MICRO_PROFIT_OCF_PS_MIN=0.3`（每股经营现金流下限）+ `MICRO_PROFIT_NET_MARGIN_MIN=5.0`
+ `MICRO_PROFIT_CAP=50.0`，两者同时满足才触发。实测金牛式微利 **76.9 → 50.0**，
正常公司 62.2 不受影响。净利率由 engine 在构造 ctx **前**从 fin_df 直接算
（`net_profit/revenue` 最新一期，与盈利模块 `dupont.net_margin` 同源）——**不能靠模块循环回填**
（ctx 在循环前构建，cashflow 在循环内运行，回填不到）。

### 修复 B：估值软化增加分位交叉校验（engine.py）

`PE>80 → signal="合理"` 此前无条件生效。现 `SOFTEN_MAX_PCTL=90`：分位≥90 改判「偏高」，
明示「成长预期已被充分定价」；<90 维持原软化。

### 修复 C：分类准入门槛（market_scan.py，读层）

**不是一票否决**：只把档位封顶「观察」，不改写 composite_score/技术面（不产生第二个总分）。
四类各用各门槛：红利（股息≥3.5 且 payout≥50，不看 ROE/周转）/ 成长（营收≥15 或毛利≥30，
不看 PE 绝对值）/ 周期（仅 cycle_trap）/ 传统价值（**矛盾组合** PE分位≥80 且 ROE<6，
或极端兜底 周转<0.15 / PE分位≥97 且 ROE<12）。周期型置于成长型**之前**判定。
实测区分正确：金牛化工拦、大秦铁路放行、中国神华放行、三环集团放行、义翘神州放行、僵尸资产拦。

### 踩坑（重要，已加回归测试）

- **`float(None)` 让共振索引构建整体失败**：成长型分支格式化文案直接
  `float(revenue_yoy)`，当营收 None 而毛利率有值（或反之）且均不达标 → TypeError →
  索引构建 error → `/technical-overlay` 恒 503。已改为 None 安全文案（显示「缺失」）。
- **SQL 与代码字段不同步**：`_base_items` 读了 `revenue_yoy` 但 `_LIGHT_SQL` 没选它
  → 恒 None，成长型营收判据形同虚设。已补 SQL 与 `_from_payload` 回退路径。
  **教训：给读层加字段必须同时改 SQL + _from_payload + 代码三处**。
- 指标位置实测稳定：`prof[0]=ROE / prof[1]=毛利率 / eff[0]=总资产周转率`（5254 只）。

### 部署与状态

- `SCORING_VERSION` `.4 → .5`（A/B 改模块分与估值分，必须全量重建）。
- 新增测试 13 例（engine 4 + market_scan 9），全量 **400 passed / 5 failed(既有) / 1 skipped**。
- 服务器 09:37 部署、09:37 重启服务；索引构建因上述 bug 失败 → 已修复并重新部署。
- `.5` 全量重建已启动（首查 39 只）。定时任务 `079290fc`（每 3h 续跑，validUntil 09-23）。

## 第八轮：线上验证暴露闸门两个真洞（scoring_version .5 → .6）

`.5` 部署后重新触发共振索引构建 —— **`float(None)` bug 确认修好**（`status:done / pct:100 / error:null`，
此前秒失败）。但**线上榜单核验发现闸门有两个洞，其中一个正是用户最初投诉的金牛化工**。

### 洞一：周期型是免检通道（严重）

- `classify_admission_gate` 周期分支只判 `cycle_trap`，而该参数在唯一调用点
  （`_base_items` L364）**硬编码 `cycle_trap=False`**，注释还写「见 `_overlay` 层补充」
  但 `_overlay` 从未补充 → **894 只强周期股全部无条件放行**。
- 实测混入 **143 只「PE分位≥80 且 ROE<6」**，与金牛化工同病：
  泸天化 99.3/0.5、招商蛇口 100.0/0.73、华菱钢铁 99.2/4.77、山西焦煤 95.9/3.33、冀中能源 87.2/3.07。
- **金牛化工本体（600722，化学原料 ∈ STRONG_CYCLICAL）就走这条路放行**，
  榜单 `profile_gate=null` / `verdict=candidate`，PE_TTM 193.52 / PB 8.49。
- 修复：周期分支补「估值↔盈利矛盾」判据（PE分位≥80 且 ROE<6 → 拦），
  与「传统价值型」同尺；**但仍豁免真·谷底反转**（PE 在低分位时 ROE 为负照常放行，
  那正是该买时点）。峰值周期股（中国神华 95.6/12.76）不受影响。

### 洞二：周转率兜底误杀整个金融业

- 兜底判据 `周转率 < 0.15` 用制造业尺子量金融：**42 家银行 + 49 家券商 + 20 家多元金融**
  共 115 只被误拦（宁波银行 0.02、华泰证券 0.033、华夏银行 0.019、苏州银行 0.016）。
  银行赚息差、券商赚佣金，总资产周转率天然 0.02~0.08，**不是低效而是业务模型**。
  这也与用户自己的原则冲突（「红利型不看 ROE 和周转率」）。
- 修复：新增 `ADMISSION_FINANCIAL_INDUSTRY_KW`（银行/保险/证券/多元金融/信托/期货）
  结构性豁免周转兜底；**豁免 ≠ 免检**，金融股仍过「矛盾组合」那一关
  （实测宁波银行 ROE 11 → 放行；ROE 0.7 / PE分位 100 → 照拦）。
- 闸门签名新增 `industry` 参数，调用点 L373 传 `ind`（注意 L482/L1369 的
  `industry=ind_filter` 是 `_item_passes` 筛选，与闸门无关，勿混）。

### 全库 5254 只影响实测（改前 vs 改后）

- 新增拦截 **160** 只（135 周期型 + 25 红利型，如万科A/大悦城/华侨城A 走 payout 判据）
- 纠正误杀 **152** 只（券商 申万宏源/广发/国元/长江、多元金融 陕国投A/中油资本、
  铁路公路 山西高速/东莞控股、电力 川能动力）
- 净 PASS **3893 → 3885**（数量几乎中性，但**靶向性大幅改善**）

### 另一个线上发现：3640/5254 快照口径混用

- 版本分布：`None`(09-20 旧口径) **3640** / `.1` 414 / `.4` 725+368 / `.5` 仅 **106** / `.3` 1。
- 指数构建 4.9s 前刷新（`index_stale=false`），但读的是**混合口径快照** ——
  金牛化工 `scoring_version=None`，所以它仍带着 09-20 的洗白估值
  （`growth_stock_profile.tier=turnaround` 旧值），**这也是闸门判据取到旧字段的原因之一**。
- 故 `.6` 除闸门修复外，另一个目的是**逼一次干净全量重建**扫掉 3640 份旧快照。

### 测试与部署

- 新增测试：周期非免检 1 例 + 金融豁免 1 例（并把原 `test_admission_gate_cyclical_only_traps_at_earnings_peak`
  改名为 `..._traps_at_earnings_peak`，语义从「仅」改为「也」）。全量 **404 passed / 5 failed(既有) / 1 skipped**。
- `SCORING_VERSION` `.5 → .6`。已部署（服务重启 + health 200）。

### 服务端口备忘（踩坑）

- **线上服务监听 `127.0.0.1:8002`，不是 8000**（`systemctl cat candle-flow` →
  `uvicorn app.main:app --host 127.0.0.1 --port 8002`）。用 8000 探测恒 HTTP 000，
  容易误判「服务挂了」。cloudflared 隧道在 20241/20242。
- 本地 `factor_snapshots` 表**存在但 0 行**（快照只在服务器构建），
  本地做影响评估必须 scp 脚本到服务器跑，且脚本里 `sys.path.insert` 要写
  `/opt/candle-flow/backend`。

## 第九轮：五层改造方案核实 + 两处修复上线（scoring_version .6 → .7）

用户提出「分层过滤 + 动态权重」五层改造方案（前置排雷一票否决 / 基本面硬门槛 /
技术面择时加校验 / 动态权重 / 四级输出）。**逐条在 5254 只全库实测**，而非纸面判断。

### 结论汇总：3 采纳 / 6 已实现 / 5 拒绝 / 5 部分可行

**采纳并已上线（`.7`）**：
1. **估值软化阶梯收紧**（`engine.py`）：`.5` 的 `SOFTEN_MAX_PCTL=90` 单边上限仍太松，
   长白山 PE 80.6 / 分位 86.1 被洗成「合理」。改为**绝对估值档位决定分位门槛**
   （PE 80~150 → <80；PE>150 → <70；PB>8 → <80），超门槛判「偏高」。
   实测：长白山→偏高、金牛 193.5/89.8→偏高、真成长 PE95/分位60→仍「合理」（未误伤）。
2. **减持公告语义召回**（`major_risk_events.py`）：立霸股份 603519 `notice_scanned=150`
   但 `events=[]` —— 标题「关于控股股东一致行动人减持股份计划公告」含「减持股份计划」
   却既不含连续的「减持计划」也不含顺序相反的「股份减持」。加 `all_of` 语义共现后
   **「新增风险」类召回 3/8 → 8/8**（15 种写法整体 5/15 → 13/15）。

**拒绝（实测会误杀，或靶点已由其他机制覆盖）**：
- `PE>80` 一刀切：全库 20.4%；TOP50 命中 6 只且**6 只全被判成长股** → 连真成长一起砍。
- `PE>行业中位数×2`：16.2%；`PEG≤1.5` 对周期股无意义；`日均成交额<5000万`：
  快照与 K 线均无成交额字段，**无法实现**。
- **基本面60%/技术面40% 加权：拒绝**。`composite_score` 是基本面单口径权威分，
  `dim_scores` 五维全是基本面维度，技术面 `tech_score` 独立并列。加权 = 造第三个总分，
  违反铁律 10（茅台实例：五维 89.7/68.3/87.0/91.1/86.0，无技术面维度）。

**已实现（早就在跑）**：经营现金流/净利润（`cashflow` 模块有「当期」+「5年均值」两口径）；
四级输出（现存 core/candidate/watch/eliminated = 用户 A/B/C/D）；ST 剔除
（`filters.exclude_st` 已开，实测 ST 命中 0 只）；量价配合（`confluence` 用当日量/20日均量
拦无量左侧形态）；硬门槛（`classify_admission_gate` 已按四类资产分设门槛）。

**部分可行（缺数据源或阈值需重设）**：质押率≥60%（字段存在但仅 32.4% 有值，
**立霸 49.99% 恰好逃逸**）；商誉/净资产≥30%（**快照无 goodwill 字段**）；
减持≥3%（标题只有「有无」无比例）；资产负债率<60%（全库 23.3%，含金融地产天然高杠杆）；
换手率>15%（快照无换手率）。

### 决定性反证（记入长期记忆）

**中国神华 PE 仅 18.6 但 PE 分位 96** → 分位高 ≠ 泡沫。
**PE 分位 95.6 拦中国神华却放走 89.8 的金牛化工** → 单指标绝对阈值对本问题完全无效。
有效判据只能是**指标间的矛盾组合**（已在 `classify_admission_gate` 落地）。

### 部署与状态

- `SCORING_VERSION` `.6 → .7`；本地全量 **427 passed / 5 failed(既有) / 1 skipped**。
- PowerShell `push.ps1` 部署成功（`SETUP_OK`），线上验证：
  `SCORING_VERSION = "2026.09.21.7"`、`all_of` 出现 6 处、服务 active。
- 线上复验两处修复均生效（立霸标题命中 reduce_hold / observe / 2026-08-25；
  长白山 signal 合理→偏高；真成长未被误伤；红利/非成长正确跳过）。
- `.7` 全量重建已启动（首查 87 只）；定时任务 `079290fc` 已改名「candle-flow .7 快照重建续跑」
  并更新 prompt（含新版本号 + 两处修复的线上抽验步骤），validUntil 延至 09-25。
- 此前 `.6` 重建被本次部署重启打断，属预期（`.7` 重建会覆盖）。

### 服务端口 / ssh 踩坑（复现）

- **同一条 ssh 里串长轮询（`sleep 30` + 查询）会被工具 SIGTERM 掐断** →
  启动与查询必须分两次 ssh 调用，且启动那句加 `sleep 2` 就够。

