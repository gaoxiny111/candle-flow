# 2026-09-21

## 技术面共振修复：空头趋势不再给「核心持仓」（002545 案例）

- 用户反馈：外部 AI 对 002545 东方铁塔的技术面分析（MA 空头排列、MACD 零下死叉、RSI 18 超卖、缩量、资金流出）与系统榜单「核心持仓·技术100·看涨十字孕线」矛盾。
- 服务器实跑复现根因：9/18 缩量十字孕线被判 90 分看涨形态，共振命中 = MACD（绿柱缩短 0.007 也算 turning_up）+ 低点（贴20日低点）+ 布林（贴下轨）→ 3 维各 1 → 组合 100 → candidate=True；日线空头排列无任何惩罚，周线因 90 根日线窗口太短判 sideways 拦不住。
- 修复 `core/confluence.py` 四处：① 空头排列(MA5<MA10<MA20) 里的 bullish 形态 → structure_flaw 淘汰（对称拦多头排列里的 bearish）；② MACD 命中要求 DIF 在 DEA 正确一侧；③ RSI 命中限 28~48 / 52~72；④ 随机指标需已金叉/死叉。已部署上线并验证：002545 candidate=False、overlay 中 pattern/tech_score=null。
- 全量测试 321 过，5 个失败均为既有已知失败。profit_yoy 回填已 0 缺失（5226 只），覆盖率 97.1%。
- 新增诊断工具：`_diag_002545.py`（单票共振链路）、`_diag_002545_replay.py` + `_dump_kline.py`（服务器导 K 线 → 本地回放）。
- MEMORY.md 已精简重写（原 85 行超注入上限 → 紧凑版）。

## K 线形态页新增「技术面分析」成文报告

- 后端 `services/tech_narrative.py`：`build_tech_narrative(db, symbol)` 用 180 根 K 线计算 MA5/10/20/60 排列、近5/20日涨跌、周线趋势、MACD（含近10日金叉死叉检测）、RSI/KDJ/布林/量能、近10日形态（PatternEngine）、极性支撑压力、近期高低点，模板化生成「一 趋势与形态 / 二 技术指标 / 三 支撑压力表 / 四 综合判断与操作建议（持仓者/空仓者/止损参考）」；对最近形态跑 `evaluate_confluence` 提取 structure_flaw 作为形态提示（与信号页同源，继承当日守卫）。
- 端点 `GET /api/v1/kline/tech-narrative?symbol=`（kline.py）。前端 `components/TechNarrative.vue`，加在 ChartView K 线形态 tab 图表下方（指数/ETF 不显示），可折叠。注意：数据源无个股资金面 → 报告不提供资金面段落并在 note 声明（铁律13）。
- 180 根窗口下 weekly_trend_at 对 002545 判 down（90 根时判 sideways）——长窗口顺带缓解了周线滞后问题。
- 线上实测 002545 输出与外部 AI 分析结论一致（空头排列、反弹目标 MA5/MA10、止损近低点 15.79）。tsc 既有错误在 FundamentalPanel/DashboardView，与新组件无关。

## 报告新增「四、资金面」段（个股资金面实锤可用）

- 用户要求把外部 AI 那种资金面分析也加进报告。先探测数据源：**推翻了铁律 13 的旧前提**——东财 `push2his/api/qt/stock/fflow/daykline/get`（klt=101）有 120 根个股日频主力资金（f52 主力=f55 大单+f56 超大单 自证），`datacenter-web` RPTA_WEB_RZRQ_GGMX 有融资融券明细（RZJME5D/10D 现成），RPT_DAILYBILLBOARD_DETAILSNEW 有龙虎榜。北向个股日频 2024-08 停披、DDX/主力控盘拿不到 → 仍不做打分/过滤，只展示。
- 新增 `services/stock_fund_flow.py` + 模型 `stock_fund_flow_daily`（90 天窗口落库）：展示层优先读库，库内无该标的才同步等一次（预算 5s），落后→后台线程增量同步（全局锁串行 + 1.5s 间隔 + 同标的 2min 节流）。
- **踩坑**：push2his 对新建连接有速率冷却（短时并发被 RST，约 15s 恢复，自己并发自己会挂）；push2delay/push2 无日频；scp 同名文件互相覆盖过一次；报告文案显式标注资金面披露日（滞后 K 线）。
- tech_narrative 报告改五段（资金面插在支撑压力之后），含 5 日主力明细表 + 资金面倾向标签（偏多/偏空/分歧，仅叙述不产生分数）；操作建议里原「暂无个股资金面数据」改为真实资金面条件（如「主力资金转为净流入并连续 3 日以上」）。
- 线上验证：002545 近 5 日主力净流出 1.66 亿、连续 8 日净流出、近 10 日融资净偿还 1356 万 → 资金面「偏空，与均线空头排列互相印证」；600519 首访同步成功（茅台近 5 日净流出 4.85 亿）。浏览器截图确认渲染。测试 321 过 5 已知失败，无回归。

## 技术分 100 的构成解释（603136 案例）

- 公式：`combined = 形态分 + 有效共振数×6`（每个 low_momentum 软冲突 -8），榜单 `tech_score = min(combined, 100)`，取近期窗口内**最高组合分**的形态。CANDIDATE_COMBINED=80。
- 603136 天目湖 104 = 9/17「破低反涨」形态分 86 + 3 共振（RSI 44 中性偏弱区 / MA5≥MA10 均线转多 / 放量 2.45 倍）×6；9/16 同形态 86+4×6=110。守卫未拦：非完整空头排列（MA10>MA20）、周线 sideways、无 soft 冲突 → candidate=True → 基本面 B(71.8) → 买入候选。
- tech 100 ≠ 满分完美：是 86 分形态+共振加成后封顶，形态本身最高约 90+。
- 600301 华锡有色「形态—/技术—」原因：近 2 根 K 线窗口内 PatternEngine 未识别出任何看涨形态（近期形态全是 bearish 且在 9/08 前，超窗）；且其 MA 空头排列+周线 down，即便出现看涨形态也会被守卫拦下。属预期行为，非缺陷。

## 基本面口径修复：新潮能源 600777（对外部 AI 质疑逐条核实）

- 外部 AI 质疑 5 点，逐条核后确认**四处真缺陷**并修复（详见 MEMORY.md「基本面口径修复」段）：① 生存级风险无释放通道（600777 已于 2026-06-19 撤销退市风险警示摘帽，公告前缀由 *ST新潮→新潮能源，系统仍强制 E + value_trap 锁 28）；② 成长「边际改善/单季加速」在净利同比 -65.6% 时仍加分（74+82，权重 6.0/16.9）；③ 重资产关键词漏「油气开采」→ 营运效率 0 分；④ risk 泛化「开采」套煤炭提示 + `financials.py` DataFrame 真值判断崩溃。
- 修复后 600777：composite 25.0/E → **58.3/C**（盈利 72、成长 29.5、现金流 77.6、偿债 85.3、营运 40、风险 97、估值 38.3），warnings 含两条「〔已解除〕…（原风险公告 …）」留痕。对照 *ST康佳A(000016) 仍 fatal=True/10.6，风控未改松。
- 新增 4 条回归测试（test_major_risk_events.py：摘帽解除 / 申请撤销不算解除 / 简称仍带 ST 拦解除 / 解除语义白名单）；测试 325 通过，5 个既有失败。部署 3 次（15:01、15:20 重启均晚于文件 mtime）。
- 新增 `SCORING_VERSION`（factor_db.py，=2026.09.21.1）口径版本戳：解决"口径改了但盘后非 force 重建跳过旧快照 → 榜单与详情两个分数"。bump 后全量 5226 只被判定待重建，已分批触发（重启会打断，需重触发；盘后 16:35 任务也会继续）。
- 残留待办：中报资产负债表仅新浪源可得，限流时静默回退年报（偿债分 92.5↔85.3、综合分 ±3 漂移）；`build_financial_dataframe` 取数全失败返回空 frame 而非报错（静默降级）——都建议后续加「数据缺口留痕」。

## 新增文档：docs/基本面榜单-共振逻辑.md

- 用户要求「把基本面榜单-共振的逻辑整理出来」。通读 `core/confluence.py`、`services/market_confluence_service.py`、`services/market_scan.py`、`api/v1/fundamentals.py`、`frontend/src/views/MarketScanView.vue` 后成文。
- 文档覆盖：三层漏斗（基本面榜只读快照 → K线买点信号并列不入分 → 形态共振）× 五维正交（每维留 1）/ 组合分公式 / 硬冲突与三类软冲突 / 双阈值判档与排序 / 两种用法（overlay 本页 vs resonance 后台索引）与缓存容错 / 与信号页同源但「软冲突处理力度」不同 / 口径边界。
- 关键补充认知（此前未记录）：**能给出 tech_score 的票组合分必然 ≥80（CANDIDATE_COMBINED），故有共振者技术分天然 ≥80，`min_tech=70` 在本数据上等价于「有形态共振」，真正区分线是 85**。
- 信号页 vs 榜单差异：`_level_from_confluence`（signal_service.py:304）对 emotion_extreme/structure_flaw 只做 strong→medium 封顶（仍出信号），榜单 `_is_candidate` 直接剔除。

## 第二轮调优落地 + 线上实测（五条意见）

- 五条意见落地结论：① RSI 部分采纳（上界 48→60、下界**不动 28**，中轴区满权/弱势区 0.6）；② K 线窗口 90→**180**；③ 放量阈值动态化 `k=clip(CV/0.35,0.85,1.25)`；④ 大盘环境**只展示不入分**（新端点 `/market-regime`，`scoring_impact=none`）；⑤ **不采纳 PB-ROE**，改修真病灶——榜单 PEG 从「PE÷单期同比」改为直读 `relative.PEG.value`（3 年 CAGR + 周期/红利豁免）。
- 关键论证：RSI 0.6 + 另一维 1.0 = 1.6 < `MIN_HITS`(2.0) → 弱势区 RSI 撑不起双证，放宽准入但不通胀组合分。
- **实测（本轮最有价值的新证据）**：周线判定窗口敏感性扫描，400 只主板票以今日为锚——90→180 根不一致 **11.5%**（以 `sideways → down` 为主）、120→180 为 5.5%、150→180 为 0.8%、**180→240 为 0.0%**。→ **180 是收敛点**，不是拍脑袋；变化方向正是要修的病（缓跌误判震荡 → 周线硬否决不触发）。
- **踩坑（已写进 skill）**：比对窗口时用 `kl[:n]` 得到 65.8% 的假差异——`kl[:90]` 是**最老的** 90 根（截至约 4.5 月前），必须 `kl[-n:]` 让所有窗口共用终点。
- 线上：索引重建 182s / 2050 只 / 0 失败；`kline_bars=180` 生效；放量自证可人工复算（`CV=1.19 → k=1.25 → 阈值 1.875≈1.88`）；组合分可复算（`67+3×6=85`）。
- 档位抽检 603136 买入候选(71.8/85) / 002545 观察(87.3/None，**空头排列守卫在 180 窗口下仍有效**) / 600301 观察(81.1/None) / 600777 淘汰(58.3/None)。D0 与 D1 逐字段一致——因 D0 所用索引已是新代码所建（16:02 那次），本次属复核。
- 全量测试权威值 **351 passed / 5 failed / 1 skipped**（既有失败：拼音 4 + 主板扫描 1，无回归）。此前文档写 349、`_test_all.txt` 是 321，均已修正。
- 文档新增第 11 节「线上实测」+ 附录脚本表（`_verify_a.sh` / `_verify_cd.sh` / `_verify_weekly.sh`）。
- **线上验证脚本固定套路**：`scp → sed -i 's/\r$//' → nohup bash ... > /root/out.txt 2>&1 &` → 另起 ssh 轮询 `grep -q '===== DONE ====='`。带索引重建的脚本塞进单条 ssh 串行必 SIGTERM。


## 第三轮：外部质疑「三个核心漏洞」逐条核实（17:00 前）

- 结论：**证据多与事实不符，但暴露 3 处真缺陷**。
- 缺陷①**估值分对 PE 不单调**：`growth_profile` 有自证循环 `PE>80 → 判成长 → 估值软化`，覆盖 99.3%（1068/1076）的 PE>80 票，`pe_distorted` 档估值中位 41.0 > 普通股 28.0。→ 删除该规则。外部提议的 `PE>80 → composite=0` 不采纳（会误杀北方华创 PE86 真成长）。
- 缺陷②**PEG 全库恒 None（静默 bug）**：`_snapshot_peg_map` 写 `$.relative.PEG.value`、`_extract_fundamental_fields` 写 `result.get("relative")`，但真实路径是 `valuation.relative`（快照与 engine 返回 dict **都没有顶层 relative**）。→ strong_buy/watch 两档硬性要求 `peg is not None`，整张榜单买点信号 100% neutral 且无报错。**旧用例手搓了顶层 relative fixture，把错误路径固化成"期望"，所以生产失灵而测试全绿**。已修 3 处 + 改为按真实嵌套结构构造夹具 + 加 sqlite 端到端用例。
- 缺陷③**turnaround 无估值前提**：`cyclical_profile.py` 是死代码（从未 import），只要 3 年 CAGR 负 + 最新同比转正就算周期底部。→ 加 `CYCLE_TROUGH_PB_MAX=5.0`（万华 2.01/璞泰来 2.23/中电港 3.29 保留；金牛 8.49/中国卫星 11.03/长裕 11.15 排除）。用 PB 而非 PE：底部 E→0 时 PE 天然巨大，用 PE 会误杀真底部。
- 外部「甬金股份是周期顶点」不成立：2026Q2 归母环比 +85%（2024Q3 以来最高），是周期修复。
- 外部把 `tech_score`（上限 100）当成形态分（75），故「压到 60 以下」的建议是抬分不是压分；空头排列的真实处置早已是 `tech_score=None`（更彻底）。
- 线上实时路径验证：金牛化工 估值 56→20、综合 71.5→65.6。SCORING_VERSION `.2 → .3`。

## 第四轮：外部质疑第二组「致命问题 1/2/3」（17:25 部署 .4）

- **致命问题 1（左侧抄底无防守）成立且最严重**。空头排列守卫只拦 `MA5<MA10<MA20`；缺口是 `MA5>MA10` 但收盘 < MA60 且当日无量。实测三只真入选：北方华创 676.61/MA60 724.61/量比 0.87 + 平底锅底部 combined 105；雅克科技 137.43/153.37/0.97 同；平安电工 88.15/91.96/0.78（看涨捉腰带 103）。→ `confluence.py` 加左侧超跌形态防守：`pattern_name ∈ LEFT_SIDE_BULLISH(17 个)` 且 收盘<MA60 且 当日量比<`VOL_CONFIRM_MIN=1.2` → `structure_flaw`（与空头排列同强度，不叠加）。恒为科技当日量比 1.55 → 放行（量能即确认）；九洲药业 MA60 上方 → 无关。
- **致命问题 3（买点信号与决策割裂）成立**。修完 PEG 后仍有残留：候选门槛 fund≥70 而 strong_buy 要求 fund≥80 → `fund 75/tech 85` 必是「候选+中性」。→ 把买点信号定义成**决策的下游**，新增 `left_side` 左侧超跌观察 / `bottom_confirm` 右侧底部企稳 / `setup_ready` 形态达标待确认，`neutral` 中文改「趋势未确认」；硬约束「凡过候选门槛者信号不停在 neutral」「被左侧否决者不能报 strong_buy」。前端 `SIGNAL_LABELS`/`signalTone`/`hasSignal` 同步。
- **致命问题 2（周期低估值陷阱）方向对但打法要改**：外部样本甬金不构成顶点（PE分位 45.6/PB分位 44.5）。四处改动：① 行业名必须**精确匹配**——线上是 `特钢Ⅱ`/`化学原料`/`化学制品`/`工业金属`，原 `_cycle_kw` 子串表只命中 86 只（1.6%）形同虚设，改用 `STRONG_CYCLICAL_INDUSTRIES`(28 行业/919 只)；② 加 **PB 历史分位**做辨伪项（只看 PE 会把真底部当顶点）；③ 折减 **12 分**且与现金流折减取 **max 不叠加**（外部要 −20）；④ 缺数据不猜。
- 判据 `engine.cycle_trap_hit(pe_pct,pb_pct,roe)`：PE 分位≤30 且 PB 分位≥70 且 ROE≥15。全库 919 只强周期命中 **20 只（2.2%）**，集中在贵金属/工业金属景气高点（紫金 PE分位6/PB分位82/ROE33、中国铝业 4/71/17.6、云铝 2/80/19.7）。反例全对：万华 27/7.4 不中、宝钢 63.6/10.1 不中、甬金 45.6/44.5 不中、金牛 89.8/98.8 不中。
- **踩坑记牢**：快照 payload 里 `valuation.score` **不存在**，真名是 `valuation.composite_valuation_score`；`market.roe` 不存在，ROE 在 `modules.profitability.indicators[0]`（name 以 ROE 开头）。诊断脚本因此一度报「估值分 None」误导判断——先 dump payload 结构再写判据。
- 新增 20 条回归测试（夹具先自证落在缺口里、延续形态不受左侧闸门约束、过门槛不为 neutral、周期陷阱四组判别）。全量 **385 passed / 5 failed(既有)**。
- 验证脚本：`_diag_claim5.sh` / `_diag_cycle2.sh` / `_diag_taxonomy.sh` / `_diag_payload2.sh` / `_verify_claim4.sh`。文档新增 §12、§13 并改写 §3.5/§4/§5。
- SCORING_VERSION `.3 → .4`，已部署（17:23:18 重启，health 200，前端 dist 已换 `MarketScanView-DX8T2rHR.js`）；`.4` 全量重建 17:24 启动（5254/5254 待重建，约 11~13h，脚本 `/tmp/_rebuild_all.sh`，30 轮 × budget 3300s）。
